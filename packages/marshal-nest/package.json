{
  "name": "@super-hornet/marshal-nest",
  "version": "1.0.0-alpha.0",
  "description": "Marshal: A plugin for NestJS to convert HTTP Body to your class instance with validation",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": false,
  "scripts": {
    "test": "jest --coverage",
    "tsc": "tsc",
    "tsc-watch": "rm -rf dist && tsc --watch"
  },
  "peerDependencies": {
    "@nestjs/common": "^7.0.0",
    "@super-hornet/marshal": "^1.0.0-alpha.0"
  },
  "devDependencies": {
    "@nestjs/common": "^7.0.0",
    "@super-hornet/marshal": "^1.0.0-alpha.0",
    "reflect-metadata": "^0.1.13"
  },
  "jest": {
    "transform": {
      "^.+\\.(ts|tsx)$": "ts-jest"
    },
    "testMatch": [
      "**/tests/**/*.spec.ts"
    ]
  }
}
