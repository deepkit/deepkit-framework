{
  "name": "template",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/template/src",
  "projectType": "library",
  "targets": {
    "rollup": {
      "executor": "@nx/rollup:rollup",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/{projectRoot}",
        "main": "{projectRoot}/src/index.ts",
        "tsConfig": "{projectRoot}/tsconfig.build.json",
        "rollupConfig": "packages/rollup.config.js",
        "project": "{projectRoot}/package.json",
        "additionalEntryPoints": [
          "{projectRoot}/src/jsx-runtime.ts"
        ],
        "format": [
          "esm",
          "cjs"
        ],
        "generateExportsField": true,
        "external": "all",
        "compiler": "tsc"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "commands": [
          "nx run template:rollup",
          "cp {projectRoot}/src/jsx-runtime.js dist/{projectRoot}/jsx-runtime.js"
        ],
        "parallel": false
      }
    },
    "release": {
      "command": "release-it --config={projectRoot}/.release-it.json"
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/template/**/*.ts",
          "packages/template/package.json"
        ]
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "dependsOn": [
        "build"
      ],
      "options": {
        "jestConfig": "packages/template/jest.config.ts",
        "passWithNoTests": true
      },
      "configurations": {
        "ci": {
          "ci": true,
          "codeCoverage": true
        }
      }
    }
  },
  "tags": [
    "template",
    "package"
  ],
  "implicitDependencies": [
    "type-compiler"
  ]
}